{
  "name": "loopy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first workflow runtime, recorder, extractor, and visual debugger for coding agents.",
  "license": "Apache-2.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run build:contracts && bun run typecheck",
    "build:contracts": "tsc -p packages/contracts/tsconfig.json",
    "check": "bun run fmt:check && bun run lint && bun run typecheck && bun run test:contracts && bun run test:runtime && bun run test:storage && bun run test:tracing && bun run test:extractor && bun run test:cli",
    "fmt": "biome format --write .",
    "fmt:check": "biome format .",
    "lint": "biome check .",
    "test": "bun test",
    "test:contracts": "bun test packages/contracts/test",
    "test:runtime": "bun test packages/runtime/test",
    "test:storage": "bun test packages/storage/tests",
    "test:tracing": "bun test packages/tracing/tests",
    "test:cli": "vitest run packages/cli/tests",
    "test:extractor": "bun test packages/extractor/tests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "bun-types": "1.3.9",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  }
}
